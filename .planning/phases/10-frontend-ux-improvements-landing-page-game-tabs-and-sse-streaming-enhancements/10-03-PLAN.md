---
phase: 10-frontend-ux-improvements-landing-page-game-tabs-and-sse-streaming-enhancements
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified: []
autonomous: false
requirements:
  - FE10-LANDING
  - FE10-TABS
  - FE10-SSE-SPEC
  - FE10-SSE-FILE
  - FE10-CHAT

must_haves:
  truths:
    - "Landing page looks visually appealing with moon/water background"
    - "Full flow works: landing -> submit -> chat feedback -> game loads -> tabs work"
    - "Multiple games can be created and switched between"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete frontend UX flow end-to-end in a browser with a running backend.

Purpose: Visual and interactive verification that cannot be covered by unit tests — CSS rendering, SSE event timing, iframe loading, tab switching UX.

Output: Confirmed working frontend or list of issues to fix.
</objective>

<execution_context>
@/Users/albertluo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/albertluo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-frontend-ux-improvements-landing-page-game-tabs-and-sse-streaming-enhancements/10-01-SUMMARY.md
@.planning/phases/10-frontend-ux-improvements-landing-page-game-tabs-and-sse-streaming-enhancements/10-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete frontend UX overhaul: landing page with moon/water background, game tabs for multi-session management, and enhanced SSE streaming messages (spec info, file progress, inline controls).
  </what-built>
  <how-to-verify>
    1. Start the backend: `cd /Users/albertluo/other/moonpond/backend && uv run uvicorn backend.main:app --reload`
    2. Start the frontend: `cd /Users/albertluo/other/moonpond/frontend && npm run dev`
    3. Open http://localhost:3000

    **Landing Page:**
    - Verify you see a full-screen landing page with a moon/water-reflection background
    - Verify there is a centered prompt input with translucent example prompt chips below
    - Click an example chip — it should fill the prompt input
    - Submit a prompt — the landing page should disappear and the main app layout should appear

    **SSE Streaming Enhancements:**
    - Watch the chat panel during generation
    - After a few seconds, a "spec info" message should appear showing the game title and description
    - As files are generated, each file should appear as a message with filename and byte size
    - When generation completes, controls should appear inline in the chat message stream (not as a separate floating block)

    **Game Tabs:**
    - After the first game completes, verify a tab appears at the top of the chat column with the game title
    - Click the "+" button to create a new game session
    - The chat should clear and you can submit a new prompt
    - After the second game completes, verify both tabs are visible
    - Click between tabs — the chat messages and game viewer should switch to show the selected game

    **Edge Cases:**
    - Verify the first game's iframe persists when switching tabs (no reload)
    - Verify error states still display correctly in chat
  </how-to-verify>
  <resume-signal>Type "approved" if everything looks good, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
Human confirms all three features work visually and interactively in the browser.
</verification>

<success_criteria>
- Landing page is visually appealing with moon/water background
- Example prompt chips work
- SSE streaming shows spec info, file progress, and inline controls
- Game tabs support multi-session switching
- No visual regressions from previous frontend state
</success_criteria>

<output>
After completion, create `.planning/phases/10-frontend-ux-improvements-landing-page-game-tabs-and-sse-streaming-enhancements/10-03-SUMMARY.md`
</output>
