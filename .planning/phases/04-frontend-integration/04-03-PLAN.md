---
phase: 04-frontend-integration
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified: []
autonomous: false
requirements: [FE-01, FE-02, FE-03, FE-04, FE-05, FE-06, FE-07, FE-08]

must_haves:
  truths:
    - "Two-column layout renders correctly at 1280px viewport"
    - "Full generation flow works end-to-end: prompt to playable game"
  artifacts: []
  key_links: []
---

<objective>
Visual and functional verification of the complete frontend integration — confirm the two-column layout, real-time SSE progress, game iframe loading, and error handling all work correctly in a browser.

Purpose: The frontend was built entirely by Claude without visual feedback. This checkpoint confirms the user experience matches expectations before marking the phase complete.

Output: User-verified working application or a list of issues to fix.
</objective>

<execution_context>
@/Users/albertluo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/albertluo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-frontend-integration/04-CONTEXT.md
@.planning/phases/04-frontend-integration/04-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify complete frontend integration in browser</name>
  <files></files>
  <action>
Present the verification steps to the user and wait for approval. Start both servers if not already running:
- Backend: `cd backend && uvicorn backend.main:app --reload --port 8000`
- Frontend: `cd frontend && npm run dev`
  </action>
  <what-built>Complete frontend application: two-column layout with ChatPanel (prompt input, SSE stage bubbles, controls legend, error display) and GameViewer (idle placeholder, shimmer skeleton, game iframe). Connected to FastAPI backend via direct fetch + EventSource.</what-built>
  <how-to-verify>
1. Start the backend: `cd backend && uvicorn backend.main:app --reload --port 8000`
2. Start the frontend: `cd frontend && npm run dev`
3. Open http://localhost:3000 in a browser at 1280px+ width

**Layout check (FE-01):**
- Verify two-column layout: ChatPanel on left (~420px), GameViewer filling the right
- Verify dark theme: dark background, light text, accent colors
- Verify the game viewer area is prominently sized (hero element)

**Generation flow (FE-02 through FE-05):**
- Type a prompt like "A simple platformer where you jump between clouds" and click Generate
- Watch for stage bubbles appearing one by one in the ChatPanel (should see ~5 stages)
- Verify the GameViewer shows a shimmer/pulse skeleton during generation
- When generation completes, verify the game iframe loads and the skeleton disappears
- Verify the game is interactive in the iframe (try keyboard controls)

**Controls legend (FE-06):**
- After game loads, check if a controls legend card appears in the ChatPanel
- Should show key-action pairs (e.g., "Arrow keys to Move", "Space to Jump")

**Error handling (FE-07):**
- (Optional) If you can trigger an error (e.g., stop the backend mid-generation), verify an error message appears in the chat

**Reset (FE-08):**
- After game loads, verify the prompt input is cleared and re-enabled
- Try submitting a new prompt to confirm a fresh generation starts
  </how-to-verify>
  <verify>User confirms visual and functional correctness</verify>
  <done>User types "approved" confirming the application works correctly, or describes issues to fix</done>
  <resume-signal>Type "approved" if the application looks and works correctly, or describe any issues you see</resume-signal>
</task>

</tasks>

<verification>
User confirms visual and functional correctness of the complete frontend integration.
</verification>

<success_criteria>
- User approves the two-column layout and dark theme
- User confirms the full generation flow works (prompt to stages to game)
- User confirms error handling is visible and non-blocking
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-integration/04-03-SUMMARY.md`
</output>
