---
phase: 05.2
slug: fix-pipeline-generation-failure-modes
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-18
---

# Phase 05.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.x |
| **Config file** | backend/pyproject.toml |
| **Quick run command** | `cd backend && uv run pytest backend/backend/tests/ -x -q` |
| **Full suite command** | `cd backend && uv run pytest backend/backend/tests/ -v` |
| **Estimated runtime** | ~15 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd backend && uv run pytest backend/backend/tests/ -x -q`
- **After every plan wave:** Run `cd backend && uv run pytest backend/backend/tests/ -v`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 05.2-01-01 | 01 | 1 | BUG-A | unit | `cd backend && uv run pytest backend/backend/tests/test_game_manager_generator.py -x -q` | yes | pending |
| 05.2-01-02 | 01 | 1 | BUG-B | unit | `cd backend && uv run pytest backend/backend/tests/test_wiring_generator.py -x -q` | yes | pending |
| 05.2-02-01 | 02 | 1 | BUG-C/F | unit | `cd backend && uv run pytest backend/backend/tests/test_tscn_validator.py -x -q` | new | pending |
| 05.2-03-01 | 03 | 2 | BUG-D | unit | `cd backend && uv run pytest backend/backend/tests/test_node_generator.py -x -q` | yes | pending |
| 05.2-03-02 | 03 | 2 | BUG-E | unit | `cd backend && uv run pytest backend/backend/tests/test_wiring_generator.py -x -q` | yes | pending |

*Status: pending / green / red / flaky*

---

## Wave 0 Requirements

*Existing infrastructure covers all phase requirements — pytest and test files already exist. test_tscn_validator.py is created as a new file by Plan 02.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Generated game runs without blank screen | BUG-A/C | Requires Godot runtime | Run pipeline on test prompt, open WASM in browser |
| No runtime signal errors in console | BUG-D | Requires Godot runtime | Check browser console for signal mismatch errors |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
