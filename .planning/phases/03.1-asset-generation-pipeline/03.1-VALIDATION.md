---
phase: 3.1
slug: asset-generation-pipeline
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-03-14
---

# Phase 3.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.x |
| **Config file** | backend/pyproject.toml |
| **Quick run command** | `cd backend && uv run pytest -x -q` |
| **Full suite command** | `cd backend && uv run pytest -v` |
| **Estimated runtime** | ~10 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd backend && uv run pytest -x -q`
- **After every plan wave:** Run `cd backend && uv run pytest -v`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 10 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 03.1-01-01 | 01 | 1 | ASSET-01, ASSET-05, ASSET-06 | unit (TDD) | `cd /Users/albertluo/other/moonpond/backend && uv run pytest backend/tests/test_providers.py -x -v` | Created in task | pending |
| 03.1-01-02 | 01 | 1 | ASSET-01 | unit (TDD) | `cd /Users/albertluo/other/moonpond/backend && uv run pytest backend/tests/test_dotenv.py -x -v` | Created in task | pending |
| 03.1-02-01 | 02 | 2 | ASSET-02, ASSET-03, ASSET-04 | unit (TDD) | `cd /Users/albertluo/other/moonpond/backend && uv run pytest backend/tests/test_asset_generator.py -x -v` | Created in task | pending |
| 03.1-02-02 | 02 | 2 | ASSET-01 to ASSET-06 | integration | `cd /Users/albertluo/other/moonpond/backend && uv run pytest backend/tests/ -x -v` | Existing + new | pending |

*Status: pending / green / red / flaky*

---

## Wave 0 Requirements

No separate Wave 0 needed. All plans use TDD approach — test files are created within Wave 1 and Wave 2 tasks themselves, not in a prior wave.

- [x] Test scaffolds embedded in TDD tasks (Plan 01 Task 1 creates test_providers.py, Task 2 creates test_dotenv.py)
- [x] Plan 02 Task 1 creates test_asset_generator.py as part of TDD cycle

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Generated PNG loads in Godot | ASSET-04 | Requires Godot runtime | Export via CLI, open project in Godot editor, verify texture loads |
| Image quality / relevance | ASSET-02 | Aesthetic evaluation | Run pipeline with test prompt, inspect generated images visually |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (N/A — TDD embeds tests)
- [x] No watch-mode flags
- [x] Feedback latency < 10s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
