---
phase: 3.1
slug: asset-generation-pipeline
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-14
---

# Phase 3.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.x |
| **Config file** | backend/pyproject.toml |
| **Quick run command** | `cd backend && uv run pytest -x -q` |
| **Full suite command** | `cd backend && uv run pytest -v` |
| **Estimated runtime** | ~10 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd backend && uv run pytest -x -q`
- **After every plan wave:** Run `cd backend && uv run pytest -v`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 10 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 03.1-01-01 | 01 | 1 | ASSET-01, ASSET-05, ASSET-06 | import | `cd backend && uv run python -c "from backend.providers.base import ImageProvider; from backend.providers.registry import get_image_provider; print('OK')"` | n/a (import check) | ⬜ pending |
| 03.1-01-02 | 01 | 1 | ASSET-01 | import | `cd backend && uv run python -c "from backend.providers.openai import OpenAIImageProvider; from backend.providers.google import GoogleImageProvider; print('OK')"` | n/a (import check) | ⬜ pending |
| 03.1-02-01 | 02 | 2 | ASSET-02, ASSET-03, ASSET-04 | import | `cd backend && uv run python -c "from backend.stages.asset_generator import run_asset_generator; print('OK')"` | n/a (import check) | ⬜ pending |
| 03.1-02-02 | 02 | 2 | ASSET-01 to ASSET-06 | unit | `cd backend && uv run pytest backend/backend/tests/test_asset_generator.py backend/backend/tests/test_providers.py -v` | ❌ W2 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Wave 1 tasks use import-only verification. Test files are created in Wave 2.

- [ ] `uv add openai google-genai python-dotenv` — add image gen SDK and dotenv dependencies (Plan 01, Task 1)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Generated PNG loads in Godot | ASSET-04 | Requires Godot runtime | Export via CLI, open project in Godot editor, verify texture loads |
| Image quality / relevance | ASSET-02 | Aesthetic evaluation | Run pipeline with test prompt, inspect generated images visually |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 10s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
