---
phase: 05-pipeline-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/backend/stages/contract_models.py
  - backend/backend/pipelines/contract/__init__.py
  - backend/backend/pipelines/contract/pipeline.py
  - backend/backend/tests/test_contract_models.py
  - backend/backend/tests/test_contract_pipeline.py
autonomous: true
requirements:
  - OPT-01
  - OPT-07

must_haves:
  truths:
    - "GameContract and NodeContract models validate well-formed contract JSON"
    - "RichGameSpec model captures entity-level detail beyond GameSpec"
    - "ContractPipeline skeleton satisfies GamePipeline Protocol"
  artifacts:
    - path: "backend/backend/stages/contract_models.py"
      provides: "RichGameSpec, NodeContract, GameContract Pydantic models"
      exports: ["RichGameSpec", "NodeContract", "GameContract"]
    - path: "backend/backend/pipelines/contract/pipeline.py"
      provides: "ContractPipeline class skeleton"
      exports: ["ContractPipeline"]
    - path: "backend/backend/tests/test_contract_models.py"
      provides: "Unit tests for contract data models"
    - path: "backend/backend/tests/test_contract_pipeline.py"
      provides: "Test scaffold for pipeline integration"
  key_links:
    - from: "backend/backend/pipelines/contract/pipeline.py"
      to: "backend/backend/stages/contract_models.py"
      via: "imports GameContract, RichGameSpec"
      pattern: "from backend\\.stages\\.contract_models import"
    - from: "backend/backend/pipelines/contract/pipeline.py"
      to: "backend/backend/pipelines/base.py"
      via: "satisfies GamePipeline Protocol"
      pattern: "async def generate"
---

<objective>
Define the typed data contracts (RichGameSpec, NodeContract, GameContract) and ContractPipeline skeleton that all subsequent plans build against.

Purpose: Establish the interface-first foundation. Every subsequent stage implementation imports these models. The pipeline skeleton proves Protocol compliance before stages are wired.
Output: contract_models.py with all Pydantic models, ContractPipeline skeleton, test scaffolds for both.
</objective>

<execution_context>
@/Users/albertluo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/albertluo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pipeline-optimization/05-RESEARCH.md

<interfaces>
<!-- Existing contracts the executor needs -->

From backend/backend/pipelines/base.py:
```python
class ProgressEvent(BaseModel):
    type: str  # stage_start | stage_complete | error | done
    message: str
    data: dict = {}

class GameResult(BaseModel):
    job_id: str
    wasm_path: str
    controls: list[dict] = []

EmitFn = Callable[[ProgressEvent], Awaitable[None]]

class GamePipeline(Protocol):
    async def generate(
        self, prompt: str, job_id: str, emit: EmitFn,
        *, save_intermediate: bool = True,
    ) -> GameResult: ...
```

From backend/backend/stages/models.py:
```python
class ControlMapping(BaseModel):
    key: str
    action: str

class VisualStyle(BaseModel):
    palette: str
    shader: str
    mood: str

class GameSpec(BaseModel):
    title: str
    genre: str
    mechanics: list[str]
    visual_hints: list[str]

INPUT_ACTIONS = ["move_left", "move_right", "move_up", "move_down", "jump", "shoot", "interact", "pause"]
```

From backend/backend/pipelines/multi_stage/pipeline.py:
```python
class MultiStagePipeline:
    def __init__(self, *, skip_polish: bool = True) -> None:
        self._client = AsyncAnthropic()
    async def generate(self, prompt: str, emit: EmitFn, *, save_intermediate: bool = True) -> GameResult: ...
```

Note: MultiStagePipeline.generate() signature does NOT include job_id but GamePipeline Protocol does. The ContractPipeline should follow the Protocol signature (with job_id).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create contract data models with tests</name>
  <files>backend/backend/stages/contract_models.py, backend/backend/tests/test_contract_models.py</files>
  <behavior>
    - Test: RichGameSpec validates with title, genre, mechanics, entities, interactions, scene_structure fields
    - Test: NodeContract validates with script_path, node_type, description; scene_path defaults to None; methods/signals/groups/dependencies default to empty lists
    - Test: GameContract validates with title, nodes list, game_manager_enums dict, autoloads list, main_scene, control_scheme, controls list, visual_style dict
    - Test: NodeContract with empty dependencies list is identified as a leaf node
    - Test: GameContract.model_validate() on a realistic JSON blob succeeds
    - Test: Invalid GameContract (missing title) raises ValidationError
  </behavior>
  <action>
Create `backend/backend/stages/contract_models.py` with three Pydantic models:

1. **RichGameSpec(BaseModel)** — output of Stage 1 (Spec Expander):
   - `title: str` — game title
   - `genre: str` — genre classification
   - `mechanics: list[str]` — core gameplay mechanics
   - `visual_hints: list[str]` — visual style hints
   - `entities: list[dict]` — entity definitions with name, type, behavior description
   - `interactions: list[str]` — how entities interact (collisions, signals, etc.)
   - `scene_structure: str` — text description of the scene layout
   - `win_condition: str`
   - `fail_condition: str`

2. **NodeContract(BaseModel)** — per-node interface contract:
   - `script_path: str` — e.g. "player.gd"
   - `scene_path: str | None = None` — e.g. "Player.tscn", None if script-only
   - `node_type: str` — Godot node type: "CharacterBody2D", "Node2D", etc.
   - `description: str` — what this node does
   - `methods: list[str] = []` — public method signatures
   - `signals: list[str] = []` — signal names this node emits
   - `groups: list[str] = []` — groups this node belongs to
   - `dependencies: list[str] = []` — script_paths this node references

3. **GameContract(BaseModel)** — full interface contract (Stage 2 output):
   - `title: str`
   - `nodes: list[NodeContract]`
   - `game_manager_enums: dict[str, list[str]] = {}` — enum name -> values
   - `game_manager_properties: list[str] = []`
   - `autoloads: list[str] = []` — additional autoload paths beyond GameManager
   - `main_scene: str = "Main.tscn"`
   - `control_scheme: str`
   - `controls: list[dict] = []` — same ControlMapping shape
   - `visual_style: dict = {}` — palette, shader, mood

Write tests first in test_contract_models.py, then implement the models.
  </action>
  <verify>
    <automated>cd /Users/albertluo/other/moonpond/backend && uv run pytest backend/tests/test_contract_models.py -v -x</automated>
  </verify>
  <done>All three Pydantic models importable from contract_models.py, all validation tests pass including edge cases (empty deps = leaf, missing required field = ValidationError)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ContractPipeline skeleton and test scaffold</name>
  <files>backend/backend/pipelines/contract/__init__.py, backend/backend/pipelines/contract/pipeline.py, backend/backend/tests/test_contract_pipeline.py</files>
  <behavior>
    - Test: ContractPipeline() can be instantiated
    - Test: ContractPipeline.generate() signature matches GamePipeline Protocol (prompt, job_id, emit, save_intermediate)
    - Test: Calling generate() with stub stages emits at least one ProgressEvent and returns a GameResult
  </behavior>
  <action>
Create `backend/backend/pipelines/contract/__init__.py` (empty).

Create `backend/backend/pipelines/contract/pipeline.py`:
- `class ContractPipeline` with `__init__(self)` that creates an `AsyncAnthropic()` client
- `async def generate(self, prompt, job_id, emit, *, save_intermediate=True) -> GameResult`
- The generate method should currently:
  1. Emit a stage_start ProgressEvent ("Starting contract pipeline...")
  2. Emit a done ProgressEvent with the job_id
  3. Emit None sentinel
  4. Return a placeholder GameResult (job_id=job_id, wasm_path="", controls=[])
- Import from `backend.pipelines.base` for ProgressEvent, GameResult, EmitFn
- Import from `backend.stages.contract_models` for GameContract, RichGameSpec (even though not yet used — proves the import chain works)
- Add TODO comments marking where each stage will be called: spec_expander, contract_generator, node_generator, wiring_generator, exporter

Create `backend/backend/tests/test_contract_pipeline.py` with:
- test_contract_pipeline_instantiates
- test_contract_pipeline_generate_returns_game_result (using an async mock emit)
- test_contract_pipeline_emits_progress (verify emit was called with ProgressEvent)

Note: The MultiStagePipeline.generate() signature omits job_id but the GamePipeline Protocol includes it. Check the actual Protocol signature in base.py and match it exactly. If there is a mismatch (Protocol has job_id but MultiStage doesn't), follow the Protocol — ContractPipeline should accept job_id.
  </action>
  <verify>
    <automated>cd /Users/albertluo/other/moonpond/backend && uv run pytest backend/tests/test_contract_pipeline.py -v -x</automated>
  </verify>
  <done>ContractPipeline instantiable, generate() returns GameResult, emits ProgressEvents, Protocol-compatible signature. Full test suite (including existing tests) still passes.</done>
</task>

</tasks>

<verification>
cd /Users/albertluo/other/moonpond/backend && uv run pytest backend/tests/ -x -q --timeout=10
</verification>

<success_criteria>
- contract_models.py exports RichGameSpec, NodeContract, GameContract
- ContractPipeline skeleton satisfies GamePipeline Protocol
- All new tests pass, all existing tests unbroken
- Import chain works: pipeline.py -> contract_models.py -> base.py
</success_criteria>

<output>
After completion, create `.planning/phases/05-pipeline-optimization/05-01-SUMMARY.md`
</output>
