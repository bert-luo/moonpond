---
phase: 09-add-3d-game-generation-support-to-agentic-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/backend/pipelines/agentic/models.py
  - backend/backend/pipelines/agentic/spec_generator.py
  - godot/templates/base_3d/export_presets.cfg
  - godot/templates/base_3d/default_bus_layout.tres
  - godot/templates/base_3d/project.godot
  - godot/templates/base_3d/assets/shaders/glow.gdshader
  - godot/templates/base_3d/assets/shaders/chromatic_aberration.gdshader
  - godot/templates/base_3d/assets/palettes/
  - godot/templates/base_3d/assets/particles/
  - godot/templates/base_3d/.godot/
  - backend/backend/tests/test_agentic_models.py
autonomous: true
requirements:
  - 3D-SCHEMA
  - 3D-SPEC
  - 3D-TEMPLATE

must_haves:
  truths:
    - "AgenticGameSpec accepts perspective='2D' (default) and perspective='3D'"
    - "AgenticGameSpec rejects invalid perspective values"
    - "Existing specs without perspective field still validate (backward compat)"
    - "Spec generator tool schema includes perspective as required enum property"
    - "Spec generator system prompt instructs LLM to determine 2D vs 3D"
    - "base_3d template directory exists with export_presets.cfg and 3D-compatible assets"
    - "base_3d template does NOT contain 2D-only control snippets or pixel_art/scanlines/screen_distortion shaders"
  artifacts:
    - path: "backend/backend/pipelines/agentic/models.py"
      provides: "AgenticGameSpec with perspective field"
      contains: "perspective: Literal"
    - path: "backend/backend/pipelines/agentic/spec_generator.py"
      provides: "Spec generator with perspective in tool schema and prompt"
      contains: "perspective"
    - path: "godot/templates/base_3d/export_presets.cfg"
      provides: "WASM export config for 3D template"
    - path: "godot/templates/base_3d/assets/shaders/glow.gdshader"
      provides: "3D-compatible shader (canvas_item for HUD overlays)"
  key_links:
    - from: "backend/backend/pipelines/agentic/spec_generator.py"
      to: "backend/backend/pipelines/agentic/models.py"
      via: "AgenticGameSpec.model_validate() on tool output including perspective"
      pattern: "model_validate.*tool_block"
---

<objective>
Add the `perspective` field to the agentic pipeline schema and spec generator, and create the base_3d Godot template directory.

Purpose: Establishes the data contract (perspective field) that flows through the entire pipeline, and the 3D template that the exporter will copy during 3D game export. These are the foundations that Plan 02 builds on.

Output: Updated models.py with perspective field, updated spec_generator.py with perspective in tool schema/prompt, new godot/templates/base_3d/ directory, and tests for the schema changes.
</objective>

<execution_context>
@/Users/albertluo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/albertluo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-add-3d-game-generation-support-to-agentic-pipeline/09-CONTEXT.md
@.planning/phases/09-add-3d-game-generation-support-to-agentic-pipeline/09-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From backend/backend/pipelines/agentic/models.py:
```python
class AgenticGameSpec(BaseModel):
    title: str
    genre: str
    mechanics: list[str]
    entities: list[dict]
    scene_description: str
    win_condition: str
    fail_condition: str
    # ADD: perspective: Literal["2D", "3D"] = "2D"
```

From backend/backend/pipelines/agentic/spec_generator.py:
```python
SPEC_SYSTEM_PROMPT = """..."""  # Add "Determine whether the game is 2D or 3D" instruction
SUBMIT_SPEC_TOOL = { ... }     # Add "perspective" property with enum: ["2D", "3D"], mark required
async def run_spec_generator(client, prompt, emit) -> AgenticGameSpec
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add perspective field to AgenticGameSpec and update spec generator</name>
  <files>backend/backend/pipelines/agentic/models.py, backend/backend/pipelines/agentic/spec_generator.py, backend/backend/tests/test_agentic_models.py</files>
  <action>
1. In `models.py`, add `perspective: Literal["2D", "3D"] = "2D"` to `AgenticGameSpec`. The `Literal` import already exists. Place it after `fail_condition`. Default `"2D"` ensures backward compatibility.

2. In `spec_generator.py`, update `SPEC_SYSTEM_PROMPT`:
   - Add a line to the "Your job:" list: `"- Determine whether the game is 2D or 3D based on the concept."`
   - Change entity type description on line 66 from `"Godot node type (CharacterBody2D, Area2D, etc.)."` to `"Godot node type — use 2D types (CharacterBody2D, Area2D, StaticBody2D) for 2D games or 3D types (CharacterBody3D, Area3D, Node3D, Camera3D, MeshInstance3D) for 3D games."`

3. In `spec_generator.py`, update `SUBMIT_SPEC_TOOL`:
   - Add `"perspective"` property to the `"properties"` dict:
     ```python
     "perspective": {
         "type": "string",
         "enum": ["2D", "3D"],
         "description": "Whether this is a 2D or 3D game.",
     }
     ```
   - Add `"perspective"` to the `"required"` list.

4. In `test_agentic_models.py`, add tests for the perspective field:
   - Test that `AgenticGameSpec` with no perspective defaults to `"2D"`
   - Test that `AgenticGameSpec` with `perspective="3D"` validates
   - Test that `AgenticGameSpec` with `perspective="isometric"` raises `ValidationError`
   - Test that `SUBMIT_SPEC_TOOL` schema includes `perspective` in properties and required list (import from spec_generator)
  </action>
  <verify>
    <automated>cd /Users/albertluo/other/moonpond/backend && uv run pytest backend/tests/test_agentic_models.py -x -q</automated>
  </verify>
  <done>AgenticGameSpec accepts perspective="2D" (default) and "3D", rejects invalid values. SUBMIT_SPEC_TOOL schema includes perspective as required enum. Spec system prompt includes 2D/3D determination instruction and dimension-aware entity types.</done>
</task>

<task type="auto">
  <name>Task 2: Create base_3d Godot template directory</name>
  <files>godot/templates/base_3d/</files>
  <action>
Create `godot/templates/base_3d/` by copying from `base_2d` and curating for 3D:

1. Copy these files verbatim from base_2d:
   - `export_presets.cfg` (WASM export config — identical for 2D and 3D)
   - `default_bus_layout.tres` (audio bus layout)
   - `.godot/` directory (engine cache — Godot regenerates on first export)

2. Copy `project.godot` from base_2d but it does not matter much since the LLM generates project.godot — this is just a fallback. Keep it as-is from base_2d (the LLM overrides it).

3. Copy assets selectively:
   - `assets/shaders/glow.gdshader` and its `.uid` file — YES (canvas_item, usable on CanvasLayer/UI overlays in 3D)
   - `assets/shaders/chromatic_aberration.gdshader` and its `.uid` file — YES (same rationale)
   - `assets/shaders/pixel_art.gdshader` — NO (2D-only sprite concept)
   - `assets/shaders/scanlines.gdshader` — NO (2D-only screen effect)
   - `assets/shaders/screen_distortion.gdshader` — NO (uses TEXTURE_PIXEL_SIZE, 2D-only)
   - `assets/palettes/` — copy ALL (Gradient .tres files work in 3D)
   - `assets/particles/` — copy ALL (GPUParticles work in 3D context)
   - `assets/control_snippets/` — do NOT copy (2D Node2D scripts, not applicable to 3D)

Use `cp -r` for directory copies. Verify no 2D-only files snuck in.
  </action>
  <verify>
    <automated>test -f /Users/albertluo/other/moonpond/godot/templates/base_3d/export_presets.cfg && test -f /Users/albertluo/other/moonpond/godot/templates/base_3d/assets/shaders/glow.gdshader && test -f /Users/albertluo/other/moonpond/godot/templates/base_3d/assets/shaders/chromatic_aberration.gdshader && test ! -f /Users/albertluo/other/moonpond/godot/templates/base_3d/assets/shaders/pixel_art.gdshader && test ! -d /Users/albertluo/other/moonpond/godot/templates/base_3d/assets/control_snippets && test -d /Users/albertluo/other/moonpond/godot/templates/base_3d/assets/palettes && test -d /Users/albertluo/other/moonpond/godot/templates/base_3d/assets/particles && echo "PASS: base_3d template verified" || echo "FAIL: template verification failed"</automated>
  </verify>
  <done>godot/templates/base_3d/ exists with export_presets.cfg, default_bus_layout.tres, .godot/ cache, glow + chromatic_aberration shaders, all palettes, all particles. No control_snippets, no pixel_art/scanlines/screen_distortion shaders.</done>
</task>

</tasks>

<verification>
- `cd /Users/albertluo/other/moonpond/backend && uv run pytest backend/tests/test_agentic_models.py -x -q` passes
- base_3d template directory has correct file composition (verified by Task 2 automated check)
- Existing test suite still passes: `cd /Users/albertluo/other/moonpond/backend && uv run pytest backend/tests/ -x -q`
</verification>

<success_criteria>
- AgenticGameSpec.perspective field validates "2D" (default) and "3D", rejects others
- SUBMIT_SPEC_TOOL schema has perspective as required enum property
- SPEC_SYSTEM_PROMPT instructs LLM to determine 2D vs 3D
- godot/templates/base_3d/ exists with curated 3D-compatible assets
- All existing tests pass unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/09-add-3d-game-generation-support-to-agentic-pipeline/09-01-SUMMARY.md`
</output>
