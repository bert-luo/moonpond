---
phase: 08-agentic-template-decoupling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - godot/templates/base_2d/game_manager.gd
  - godot/templates/base_2d/game_manager.gd.uid
  - godot/templates/base_2d/Main.tscn
  - backend/backend/pipelines/agentic/input_map.py
  - backend/backend/pipelines/agentic/file_generator.py
  - backend/backend/tests/test_input_map.py
  - backend/backend/tests/test_file_generator_prompt.py
autonomous: true
requirements:
  - TMPL-SLIM
  - AGENT-PROJGODOT
  - AGENT-INPUTMAP
  - AGENT-ASSETS

must_haves:
  truths:
    - "expand_input_map converts simplified key names (e.g. move_left=arrow_left) to full Godot Object(InputEventKey,...) format"
    - "expand_input_map passes through lines already in Object() format unchanged"
    - "expand_input_map preserves [rendering] and [display] sections untouched"
    - "GENERATOR_SYSTEM_PROMPT no longer contains 'Do NOT generate project.godot'"
    - "GENERATOR_SYSTEM_PROMPT contains project.godot skeleton with [rendering] and [display] sections"
    - "GENERATOR_SYSTEM_PROMPT lists available asset paths (shaders, palettes, particles, control snippets)"
    - "game_manager.gd, game_manager.gd.uid, and Main.tscn no longer exist in godot/templates/base_2d/"
  artifacts:
    - path: "backend/backend/pipelines/agentic/input_map.py"
      provides: "expand_input_map utility with KEY_MAP and _EVENT_TEMPLATE"
      exports: ["expand_input_map"]
    - path: "backend/backend/pipelines/agentic/file_generator.py"
      provides: "Updated GENERATOR_SYSTEM_PROMPT with project.godot skeleton and asset paths"
      contains: "You MUST generate project.godot"
    - path: "backend/backend/tests/test_input_map.py"
      provides: "Unit tests for expand_input_map"
      min_lines: 40
    - path: "backend/backend/tests/test_file_generator_prompt.py"
      provides: "Unit tests asserting GENERATOR_SYSTEM_PROMPT content"
      min_lines: 20
  key_links:
    - from: "backend/backend/pipelines/agentic/file_generator.py"
      to: "backend/backend/pipelines/assets.py"
      via: "import SHADER_PATHS, PALETTE_PATHS, PARTICLE_PATHS, CONTROL_SNIPPET_PATHS"
      pattern: "from backend\\.pipelines\\.assets import"
---

<objective>
Create the input map expansion utility, rewrite the agentic pipeline system prompt to allow and guide project.godot generation with asset awareness, and strip dead template files.

Purpose: Eliminate the root cause of GameManager autoload mismatches and input map failures in the agentic pipeline by giving the LLM full control over project.godot configuration.
Output: input_map.py utility, updated GENERATOR_SYSTEM_PROMPT, deleted template files, test suites.
</objective>

<execution_context>
@/Users/albertluo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/albertluo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-agentic-template-decoupling/CONTEXT.md
@.planning/phases/08-agentic-template-decoupling/08-RESEARCH.md

@backend/backend/pipelines/agentic/file_generator.py
@backend/backend/pipelines/assets.py
@godot/templates/base_2d/project.godot

<interfaces>
<!-- From backend/backend/pipelines/assets.py — already exists, import these constants -->
SHADER_PATHS: dict[str, str]    # e.g. {"pixel_art": "res://assets/shaders/pixel_art.gdshader", ...}
PALETTE_PATHS: dict[str, str]   # e.g. {"neon": "res://assets/palettes/neon.tres", ...}
PARTICLE_PATHS: dict[str, str]  # e.g. {"explosion": "res://assets/particles/explosion.tscn", ...}
CONTROL_SNIPPET_PATHS: dict[str, str]  # e.g. {"mouse_follow": "res://assets/control_snippets/mouse_follow.gd", ...}
INPUT_ACTIONS: list[str]        # ["move_left", "move_right", ..., "pause"]

<!-- From backend/backend/pipelines/agentic/file_generator.py — to be modified -->
GENERATOR_SYSTEM_PROMPT: str    # System prompt for the file generation LLM
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create input_map.py with expand_input_map utility + test suite</name>
  <files>backend/backend/pipelines/agentic/input_map.py, backend/backend/tests/test_input_map.py</files>
  <behavior>
    - expand_input_map("move_left=arrow_left\nmove_right=arrow_right\n") produces correct Object(InputEventKey,...) blocks with physical_keycode 4194319 and 4194321
    - Lines already containing "Object(" are passed through unchanged (graceful passthrough)
    - Unknown key names leave the line unchanged (no crash, no data loss)
    - A full project.godot with [rendering], [display], [application], [autoload], and [input] sections preserves all non-input sections verbatim after expansion
    - Empty [input] section produces no output (no crash)
    - Mixed format (some simplified, some already expanded) handles both correctly
  </behavior>
  <action>
Create `backend/backend/pipelines/agentic/input_map.py`:

1. Define `KEY_MAP: dict[str, int]` with all keycodes from 08-RESEARCH.md Pattern 2 (arrow keys, space, enter, escape, shift, ctrl, tab, backspace, a-z, 0-9, f1-f12).

2. Define `_EVENT_TEMPLATE` string template matching the exact Object(InputEventKey,...) format from the existing template project.godot (verified in research). Use `{action}` and `{keycode}` placeholders.

3. Implement `expand_input_map(project_godot_content: str) -> str`:
   - Use regex to isolate the `[input]` section: pattern `r"\[input\]\s*\n((?:.*\n)*?)(?=\[|\Z)"` (same lookahead as wiring_generator.py).
   - For each line in the input section:
     - If line contains `Object(` or `{` -> passthrough (already expanded)
     - If line matches `action_name=key_name` (no braces) -> expand using KEY_MAP + _EVENT_TEMPLATE
     - If key_name not in KEY_MAP -> leave line unchanged
   - Replace the [input] section body with expanded lines
   - Return full project.godot content with only [input] section modified

Create `backend/backend/tests/test_input_map.py` with pytest tests covering all behaviors above. Test the round-trip of a realistic project.godot string with multiple sections.
  </action>
  <verify>
    <automated>cd /Users/albertluo/other/moonpond/backend && uv run pytest backend/tests/test_input_map.py -x -q</automated>
  </verify>
  <done>expand_input_map correctly converts simplified key format to full Godot Object() serialization, passes through already-expanded lines, preserves all other project.godot sections, and handles edge cases without crashing.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite GENERATOR_SYSTEM_PROMPT + strip template files + prompt tests</name>
  <files>backend/backend/pipelines/agentic/file_generator.py, backend/backend/tests/test_file_generator_prompt.py, godot/templates/base_2d/game_manager.gd, godot/templates/base_2d/game_manager.gd.uid, godot/templates/base_2d/Main.tscn</files>
  <action>
**A. Update `backend/backend/pipelines/agentic/file_generator.py`:**

1. Add import at top: `from backend.pipelines.assets import SHADER_PATHS, PALETTE_PATHS, PARTICLE_PATHS, CONTROL_SNIPPET_PATHS`

2. Build the asset paths section programmatically from the imported constants (not hardcoded strings). Format as shown in 08-RESEARCH.md Pattern 3, with categories: Shaders, Palettes, Particles, Control snippets.

3. Rewrite `GENERATOR_SYSTEM_PROMPT` with these changes:
   - REMOVE the line: `"- Do NOT generate project.godot, export_presets.cfg, or .import files — those come from the template."`
   - ADD affirmative instruction: `"- You MUST generate project.godot as one of your files. Do NOT generate export_presets.cfg or .import files."`
   - ADD the project.godot skeleton block (from 08-RESEARCH.md Pattern 1) with [rendering] and [display] pre-filled, [autoload] example, and [input] simplified format instructions with supported key list
   - ADD the asset paths block (built from imports above) so the LLM knows about available shaders, palettes, particles, and control snippets
   - REMOVE the line about viewport being "from the template project.godot" — the viewport size (1152x648) is now in the skeleton itself
   - KEEP all other existing rules (Godot 4 syntax, file ordering, .tscn format, etc.)

**B. Delete template files:**

Delete these three files from the repository working tree:
- `godot/templates/base_2d/game_manager.gd`
- `godot/templates/base_2d/game_manager.gd.uid`
- `godot/templates/base_2d/Main.tscn`

Use `git rm` to stage the deletions (the files are tracked in git).

**C. Create `backend/backend/tests/test_file_generator_prompt.py`:**

Test assertions on `GENERATOR_SYSTEM_PROMPT`:
- Does NOT contain "Do NOT generate project.godot"
- DOES contain "You MUST generate project.godot"
- Contains "[rendering]" and "gl_compatibility"
- Contains "[display]" and "1152" and "648"
- Contains "[autoload]" example
- Contains "[input]" simplified format example
- Contains at least one shader path from SHADER_PATHS
- Contains at least one palette path from PALETTE_PATHS
- Contains at least one particle path from PARTICLE_PATHS
- Contains at least one control snippet path from CONTROL_SNIPPET_PATHS
  </action>
  <verify>
    <automated>cd /Users/albertluo/other/moonpond/backend && uv run pytest backend/tests/test_file_generator_prompt.py -x -q && test ! -f ../godot/templates/base_2d/game_manager.gd && test ! -f ../godot/templates/base_2d/Main.tscn</automated>
  </verify>
  <done>GENERATOR_SYSTEM_PROMPT guides project.godot generation with skeleton and asset paths. Template no longer contains game_manager.gd, game_manager.gd.uid, or Main.tscn. All prompt content assertions pass.</done>
</task>

</tasks>

<verification>
- `cd /Users/albertluo/other/moonpond/backend && uv run pytest backend/tests/test_input_map.py backend/tests/test_file_generator_prompt.py -x -q` — all new tests pass
- `cd /Users/albertluo/other/moonpond/backend && uv run pytest backend/tests/ -q` — full suite still passes (no regressions)
- `test ! -f godot/templates/base_2d/game_manager.gd` — template file deleted
- `test ! -f godot/templates/base_2d/Main.tscn` — template file deleted
</verification>

<success_criteria>
- expand_input_map() correctly expands simplified key names and passes through full format
- GENERATOR_SYSTEM_PROMPT instructs LLM to generate project.godot with correct skeleton
- GENERATOR_SYSTEM_PROMPT surfaces all available asset paths
- game_manager.gd, game_manager.gd.uid, and Main.tscn removed from template
- All tests pass including full backend suite
</success_criteria>

<output>
After completion, create `.planning/phases/08-agentic-template-decoupling/08-01-SUMMARY.md`
</output>
