---
phase: 08-agentic-template-decoupling
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - backend/backend/pipelines/agentic/pipeline.py
  - backend/backend/tests/test_agentic_pipeline.py
autonomous: true
requirements:
  - PIPE-INPUTMAP

must_haves:
  truths:
    - "AgenticPipeline calls expand_input_map on generated project.godot before exporting"
    - "Expanded project.godot is written to disk and passed to the exporter"
    - "If LLM does not generate project.godot, the pipeline still completes (template fallback)"
    - "Existing agentic pipeline tests continue to pass"
  artifacts:
    - path: "backend/backend/pipelines/agentic/pipeline.py"
      provides: "Pipeline integration calling expand_input_map before export"
      contains: "expand_input_map"
    - path: "backend/backend/tests/test_agentic_pipeline.py"
      provides: "Integration test verifying input map expansion call"
      contains: "expand_input_map"
  key_links:
    - from: "backend/backend/pipelines/agentic/pipeline.py"
      to: "backend/backend/pipelines/agentic/input_map.py"
      via: "import expand_input_map"
      pattern: "from backend\\.pipelines\\.agentic\\.input_map import expand_input_map"
---

<objective>
Wire expand_input_map into the agentic pipeline so generated project.godot files have their simplified input actions expanded to full Godot format before WASM export.

Purpose: Complete the pipeline integration so the full flow works end-to-end: LLM generates project.godot with simplified input format, Python expands it, exporter produces a valid WASM.
Output: Updated pipeline.py with input map expansion call, integration test.
</objective>

<execution_context>
@/Users/albertluo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/albertluo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-agentic-template-decoupling/CONTEXT.md
@.planning/phases/08-agentic-template-decoupling/08-RESEARCH.md
@.planning/phases/08-agentic-template-decoupling/08-01-SUMMARY.md

@backend/backend/pipelines/agentic/pipeline.py
@backend/backend/tests/test_agentic_pipeline.py

<interfaces>
<!-- From plan 01 — backend/backend/pipelines/agentic/input_map.py -->
def expand_input_map(project_godot_content: str) -> str:
    """Expand simplified [input] actions to full Godot Object() format.
    Lines already in full format are passed through. Unknown key names left unchanged."""

<!-- From backend/backend/pipelines/agentic/pipeline.py — current structure -->
class AgenticPipeline:
    async def generate(self, prompt: str, job_id: str, emit: EmitFn, *, save_intermediate: bool = True) -> GameResult:
        # ... Stage 2: generate-verify-fix loop populates all_files dict ...
        # >>> INSERT expand_input_map call HERE <<<
        # ... Stage 3: Export via run_exporter() ...
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire expand_input_map into pipeline.py + add integration test</name>
  <files>backend/backend/pipelines/agentic/pipeline.py, backend/backend/tests/test_agentic_pipeline.py</files>
  <behavior>
    - When "project.godot" is in all_files after the generate-verify-fix loop, expand_input_map is called on its content
    - The expanded content is written back to both all_files dict AND to disk at project_dir / "project.godot"
    - When "project.godot" is NOT in all_files, no expansion is attempted and pipeline proceeds normally
    - The expanded project.godot is what gets passed to run_exporter (via all_files dict)
  </behavior>
  <action>
**A. Update `backend/backend/pipelines/agentic/pipeline.py`:**

1. Add import: `from backend.pipelines.agentic.input_map import expand_input_map`

2. After the generate-verify-fix loop (after the `for iteration in range(...)` block ends, before `# Stage 3: Export`), add:

```python
# Expand simplified input map to full Godot Object() format
if "project.godot" in all_files:
    all_files["project.godot"] = expand_input_map(all_files["project.godot"])
    (project_dir / "project.godot").write_text(all_files["project.godot"])
```

This ensures the exporter receives the expanded version in both the `all_files` dict and on disk.

**B. Add integration test to `backend/backend/tests/test_agentic_pipeline.py`:**

Add a test that verifies the input map expansion integration. The test should:
1. Mock `run_file_generation` to return a `generated_files` dict that includes a `project.godot` with simplified input format (e.g. `move_left=arrow_left`)
2. Mock `run_spec_generator` and `run_verifier` (as existing tests do)
3. Mock `run_exporter` to capture the `files` argument it receives
4. Assert that the `project.godot` in the files passed to `run_exporter` contains the expanded `Object(InputEventKey` format, NOT the simplified format
5. Follow the existing mock patterns in test_agentic_pipeline.py

Also add a test confirming that when `project.godot` is NOT in generated files, the pipeline completes without error (no KeyError, no expand call).
  </action>
  <verify>
    <automated>cd /Users/albertluo/other/moonpond/backend && uv run pytest backend/tests/test_agentic_pipeline.py -x -q</automated>
  </verify>
  <done>pipeline.py calls expand_input_map on project.godot before export. Integration test confirms expanded Object() format reaches the exporter. Pipeline still works when project.godot is absent from generated files.</done>
</task>

</tasks>

<verification>
- `cd /Users/albertluo/other/moonpond/backend && uv run pytest backend/tests/test_agentic_pipeline.py -x -q` — all pipeline tests pass including new ones
- `cd /Users/albertluo/other/moonpond/backend && uv run pytest backend/tests/ -q` — full suite green (no regressions)
</verification>

<success_criteria>
- expand_input_map is called in the pipeline when project.godot is generated
- Expanded content reaches the exporter (verified by integration test)
- Pipeline gracefully handles missing project.godot (template fallback)
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/08-agentic-template-decoupling/08-02-SUMMARY.md`
</output>
