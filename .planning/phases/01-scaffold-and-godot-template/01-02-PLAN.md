---
phase: 01-scaffold-and-godot-template
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - godot/templates/base_2d/project.godot
  - godot/templates/base_2d/export_presets.cfg
  - godot/templates/base_2d/Main.tscn
  - godot/templates/base_2d/game_manager.gd
  - godot/templates/base_2d/default_bus_layout.tres
autonomous: true
requirements:
  - TMPL-01
  - TMPL-05

must_haves:
  truths:
    - "base_2d/project.godot defines all 8 named input actions with physical_keycode values"
    - "base_2d/export_presets.cfg has a 'Web' preset with export_path empty (CLI path used at export time)"
    - "base_2d/Main.tscn contains only a Node2D root with no gameplay children"
    - "base_2d/game_manager.gd is registered as an autoload in project.godot"
    - "default_bus_layout.tres provides a master audio bus"
  artifacts:
    - path: "godot/templates/base_2d/project.godot"
      provides: "Engine config: app name, input map (8 actions), audio bus, autoloads"
      contains: "move_left"
    - path: "godot/templates/base_2d/export_presets.cfg"
      provides: "Web export preset — committed, never LLM-generated"
      contains: "platform=\"Web\""
    - path: "godot/templates/base_2d/Main.tscn"
      provides: "Blank main scene — Node2D root, no gameplay nodes"
      contains: "Node2D"
    - path: "godot/templates/base_2d/game_manager.gd"
      provides: "Thin autoload: palette loading, game state enum"
    - path: "godot/templates/base_2d/default_bus_layout.tres"
      provides: "Audio bus layout resource"
  key_links:
    - from: "godot/templates/base_2d/project.godot"
      to: "godot/templates/base_2d/game_manager.gd"
      via: "autoload entry in project.godot [autoload] section"
      pattern: "GameManager.*game_manager.gd"
    - from: "godot/templates/base_2d/project.godot"
      to: "8 named input actions"
      via: "[input] section in project.godot"
      pattern: "move_left|move_right|move_up|move_down|jump|shoot|interact|pause"
    - from: "godot/templates/base_2d/export_presets.cfg"
      to: "godot --headless --export-release Web"
      via: "preset name='Web' in export_presets.cfg"
      pattern: "name=\"Web\""
---

<objective>
Create the core Godot project files for the base_2d template: engine config with input map, blank main scene, committed export preset, and game_manager autoload.

Purpose: These files form the "contract" between the template and the LLM pipeline. The input actions defined here are the exact names the Code Generator must use. The export preset is committed once and never touched by the LLM.
Output: A minimal, valid Godot 4.5.1 project that can be exported to WASM (headless export tested in plan 01's test_export.sh).
</objective>

<execution_context>
@/Users/albertluo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/albertluo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffold-and-godot-template/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: project.godot with input map and autoload</name>
  <files>
    godot/templates/base_2d/project.godot
    godot/templates/base_2d/default_bus_layout.tres
  </files>
  <action>
Create `godot/templates/base_2d/project.godot`. This is the Godot 4.5.1 engine config file. It must define:
1. Application config (name, main scene)
2. Audio bus layout reference
3. All 8 input actions in [input] section
4. GameManager autoload

**project.godot full content:**
```ini
; Engine configuration file.
; It's best edited using the editor UI and not directly,
; since the parameters that go here are not all obvious.
;
; Format:
;   [section] ; section goes between []
;   param=value ; assign values to parameters

config_version=5

[application]

config/name="base_2d"
config/features=PackedStringArray("4.5")
config/icon="res://icon.svg"
run/main_scene="res://Main.tscn"
config/audio_bus_layout="res://default_bus_layout.tres"

[autoload]

GameManager="*res://game_manager.gd"

[input]

move_left={
"deadzone": 0.5,
"events": [Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":4194319,"key_label":0,"unicode":0,"echo":false,"script":null)]
}
move_right={
"deadzone": 0.5,
"events": [Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":4194321,"key_label":0,"unicode":0,"echo":false,"script":null)]
}
move_up={
"deadzone": 0.5,
"events": [Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":4194320,"key_label":0,"unicode":0,"echo":false,"script":null)]
}
move_down={
"deadzone": 0.5,
"events": [Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":4194322,"key_label":0,"unicode":0,"echo":false,"script":null)]
}
jump={
"deadzone": 0.5,
"events": [Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":32,"key_label":0,"unicode":0,"echo":false,"script":null)]
}
shoot={
"deadzone": 0.5,
"events": [Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":90,"key_label":0,"unicode":0,"echo":false,"script":null)]
}
interact={
"deadzone": 0.5,
"events": [Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":69,"key_label":0,"unicode":0,"echo":false,"script":null)]
}
pause={
"deadzone": 0.5,
"events": [Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":4194305,"key_label":0,"unicode":0,"echo":false,"script":null)]
}

[rendering]

renderer/rendering_method="gl_compatibility"
renderer/rendering_method.mobile="gl_compatibility"
```

Notes on physical_keycode values (from research — MEDIUM confidence, verify against editor if export fails):
- move_left: 4194319 (Left arrow)
- move_right: 4194321 (Right arrow)
- move_up: 4194320 (Up arrow)
- move_down: 4194322 (Down arrow)
- jump: 32 (Space)
- shoot: 90 (Z key)
- interact: 69 (E key)
- pause: 4194305 (Escape)

The `gl_compatibility` renderer is required for Web export — it is the only renderer that supports WASM export in Godot 4.

Also create `godot/templates/base_2d/default_bus_layout.tres`:
```
[gd_resource type="AudioBusLayout" load_steps=1 format=3 uid="uid://b3c5q4r2f8n7k"]

[resource]
```

The uid value can be any unique string in this format. The audio bus layout must exist at the path referenced in project.godot or Godot will log a warning during export.
  </action>
  <verify>
    <automated>grep -c "move_left\|move_right\|move_up\|move_down\|jump\|shoot\|interact\|pause" /Users/albertluo/other/moonpond/godot/templates/base_2d/project.godot</automated>
  </verify>
  <done>project.godot contains all 8 input action names (grep -c outputs 8); GameManager autoload and gl_compatibility renderer are present; default_bus_layout.tres exists</done>
</task>

<task type="auto">
  <name>Task 2: export_presets.cfg, Main.tscn, and game_manager.gd</name>
  <files>
    godot/templates/base_2d/export_presets.cfg
    godot/templates/base_2d/Main.tscn
    godot/templates/base_2d/game_manager.gd
  </files>
  <action>
Create the remaining three core template files.

**godot/templates/base_2d/export_presets.cfg:**

CRITICAL: `export_path` must be empty string. Never set an absolute path here — it breaks the Exporter stage when the template is copied to `games/{job_id}/project/`.

```ini
[preset.0]

name="Web"
platform="Web"
runnable=true
dedicated_server=false
custom_features=""
export_filter="all_resources"
include_filter=""
exclude_filter=""
export_path=""
encryption_include_filters=""
encryption_exclude_filters=""
script_export_mode=1

[preset.0.options]

custom_template/debug=""
custom_template/release=""
variant/extensions_support=false
vram_texture_compression/for_mobile=false
html/export_icon=true
html/custom_html_shell=""
html/head_include=""
html/canvas_resize_policy=2
html/focus_canvas_on_start=true
html/experimental_virtual_keyboard=false
progressive_web_app/enabled=false
```

**godot/templates/base_2d/Main.tscn:**

Blank scene — Node2D root, no children, no gameplay nodes. The LLM generates the full scene tree for each game.

```
[gd_scene load_steps=1 format=3 uid="uid://c1main2dtmpl"]

[node name="Main" type="Node2D"]
```

**godot/templates/base_2d/game_manager.gd:**

Thin autoload available globally to all generated scenes. Provides palette loading and game state tracking.

```gdscript
# game_manager.gd
# Autoloaded as GameManager — available globally to all generated scenes.
# The Visual Polisher pipeline stage sets the active palette by name.
# The Code Generator can access GameManager.get_palette_color(t) for any color needs.

extends Node

## Active color palette. Default: neon. Pipeline sets this before gameplay starts.
var active_palette: Gradient = null

## Game state for win/fail tracking. Generated scenes update this.
enum GameState { PLAYING, WON, LOST }
var state: GameState = GameState.PLAYING

func _ready() -> void:
	# Default palette: neon. Pipeline Visual Polisher overrides this.
	active_palette = load("res://assets/palettes/neon.tres")

## Set the active palette by name (matches filenames in assets/palettes/).
## Called by generated scene scripts when the Visual Polisher selects a palette.
func set_palette(palette_name: String) -> void:
	var path := "res://assets/palettes/%s.tres" % palette_name
	if ResourceLoader.exists(path):
		active_palette = load(path)
	else:
		push_warning("GameManager: palette not found: " + path)

## Sample the active palette at position t (0.0 to 1.0).
## Returns Color.WHITE if no palette is loaded.
func get_palette_color(t: float) -> Color:
	if active_palette:
		return active_palette.sample(clampf(t, 0.0, 1.0))
	return Color.WHITE

## Called by generated scenes to signal win/loss.
func set_state(new_state: GameState) -> void:
	state = new_state
```

Note: Use TAB indentation (not spaces) for GDScript — Godot's parser requires tabs.
  </action>
  <verify>
    <automated>grep -q "platform=\"Web\"" /Users/albertluo/other/moonpond/godot/templates/base_2d/export_presets.cfg && grep -q "export_path=\"\"" /Users/albertluo/other/moonpond/godot/templates/base_2d/export_presets.cfg && grep -q "Node2D" /Users/albertluo/other/moonpond/godot/templates/base_2d/Main.tscn && grep -q "active_palette" /Users/albertluo/other/moonpond/godot/templates/base_2d/game_manager.gd</automated>
  </verify>
  <done>export_presets.cfg has platform="Web" and export_path=""; Main.tscn contains Node2D root; game_manager.gd has active_palette var and set_palette/get_palette_color functions</done>
</task>

</tasks>

<verification>
After both tasks complete, run:
1. `bash scripts/verify_template.sh` — will report which files are present (many missing until plans 03-04 complete, but core files should show OK)
2. `grep -c "move_left\|move_right\|move_up\|move_down\|jump\|shoot\|interact\|pause" godot/templates/base_2d/project.godot` — should output 8
3. `grep "platform" godot/templates/base_2d/export_presets.cfg` — should show platform="Web"
4. After Godot is installed (plan 01 setup_godot.sh): `bash scripts/test_export.sh` to confirm clean WASM export
</verification>

<success_criteria>
- project.godot defines all 8 input actions with physical_keycode values and registers GameManager autoload
- export_presets.cfg has Web preset with export_path="" (empty — path provided at CLI time)
- Main.tscn is a blank Node2D with no gameplay children
- game_manager.gd implements active_palette, GameState enum, set_palette(), get_palette_color()
- All five files exist at the correct paths in godot/templates/base_2d/
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold-and-godot-template/01-02-SUMMARY.md`
</output>
