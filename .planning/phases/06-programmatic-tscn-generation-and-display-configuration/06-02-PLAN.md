---
phase: 06-programmatic-tscn-generation-and-display-configuration
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - backend/backend/pipelines/contract/pipeline.py
  - backend/backend/pipelines/contract/wiring_generator.py
  - backend/backend/pipelines/contract/node_generator.py
  - godot/templates/base_2d/project.godot
  - backend/backend/tests/test_wiring_generator.py
  - backend/backend/tests/test_contract_pipeline.py
autonomous: true
requirements:
  - TSCN-04
  - TSCN-05
  - TSCN-06

must_haves:
  truths:
    - "ContractPipeline Stage 4 uses SceneAssembler instead of an LLM call"
    - "Node generator prompt no longer says 'Also generate: {scene_path}'"
    - "Node generator prompt includes viewport size context (1152x648)"
    - "project.godot template has [display] section with 1152x648 viewport and canvas_items stretch"
    - "Full test suite passes with the wiring LLM call removed"
  artifacts:
    - path: "backend/backend/pipelines/contract/pipeline.py"
      provides: "ContractPipeline using SceneAssembler for stage 4"
      contains: "SceneAssembler"
    - path: "backend/backend/pipelines/contract/node_generator.py"
      provides: "Node generator with viewport hint, no .tscn generation instruction"
      contains: "1152x648"
    - path: "godot/templates/base_2d/project.godot"
      provides: "Template with [display] section"
      contains: "[display]"
    - path: "backend/backend/pipelines/contract/wiring_generator.py"
      provides: "Gutted wiring_generator keeping only _patch_project_godot_autoloads"
  key_links:
    - from: "backend/backend/pipelines/contract/pipeline.py"
      to: "backend/backend/pipelines/contract/scene_assembler.py"
      via: "import SceneAssembler"
      pattern: "from.*scene_assembler import SceneAssembler"
    - from: "backend/backend/pipelines/contract/pipeline.py"
      to: "backend/backend/pipelines/contract/wiring_generator.py"
      via: "import _patch_project_godot_autoloads only (not run_wiring_generator)"
      pattern: "_patch_project_godot_autoloads"
---

<objective>
Integrate SceneAssembler into the pipeline, update node generator prompts, add display configuration, and update all existing tests.

Purpose: Wire the deterministic scene assembler into the live pipeline, eliminating the last LLM call for .tscn generation and fixing viewport size hallucination.
Output: Updated pipeline.py, node_generator.py, wiring_generator.py, project.godot, and passing test suite.
</objective>

<execution_context>
@/Users/albertluo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/albertluo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-programmatic-tscn-generation-and-display-configuration/CONTEXT.md
@.planning/phases/06-programmatic-tscn-generation-and-display-configuration/06-RESEARCH.md
@.planning/phases/06-programmatic-tscn-generation-and-display-configuration/06-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs (TscnBuilder + SceneAssembler) -->

From backend/backend/pipelines/contract/tscn_builder.py:
```python
class TscnBuilder:
    def add_ext_resource(self, res_type: str, path: str) -> str: ...
    def add_sub_resource(self, res_type: str, props: dict) -> str: ...
    def add_node(self, name, node_type, parent, *, script_id=None, instance_id=None, unique_name=False, extra_props=None) -> None: ...
    def add_connection(self, signal, from_path, to_path, method) -> None: ...
    def serialize(self) -> str: ...
```

From backend/backend/pipelines/contract/scene_assembler.py:
```python
class SceneAssembler:
    def assemble(self, contract: GameContract, node_files: dict[str, str]) -> dict[str, str]: ...

def parse_onready_unique_refs(gd_source: str) -> list[dict]: ...
```

From backend/backend/pipelines/contract/pipeline.py (current, to be modified):
```python
class ContractPipeline:
    async def generate(self, prompt, job_id, emit, *, save_intermediate=True) -> GameResult: ...
```

From backend/backend/pipelines/contract/wiring_generator.py (current, to be gutted):
```python
def _patch_project_godot_autoloads(template_content: str, autoloads: list[str]) -> str: ...
async def run_wiring_generator(client, contract, generated_files, emit) -> dict[str, str]: ...
```

From backend/backend/pipelines/contract/node_generator.py (current, to be modified):
```python
def _build_node_system_prompt(node: NodeContract, contract: GameContract) -> str: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pipeline integration + prompt updates + display config</name>
  <files>backend/backend/pipelines/contract/pipeline.py, backend/backend/pipelines/contract/wiring_generator.py, backend/backend/pipelines/contract/node_generator.py, godot/templates/base_2d/project.godot</files>
  <action>
    **pipeline.py changes:**
    - Replace `from backend.pipelines.contract.wiring_generator import run_wiring_generator` with:
      `from backend.pipelines.contract.scene_assembler import SceneAssembler`
      `from backend.pipelines.contract.wiring_generator import _patch_project_godot_autoloads, TEMPLATE_DIR`
    - In generate(), replace Stage 4 (lines ~132-140):
      - Instead of calling `run_wiring_generator(self._client, contract, node_files, emit)`:
      - Emit ProgressEvent(type="stage_start", message="Assembling scene files...")
      - Call `assembler = SceneAssembler()` then `scene_files = assembler.assemble(contract, node_files)`
      - If contract.autoloads is non-empty: read template project.godot from TEMPLATE_DIR, call `_patch_project_godot_autoloads()`, add to scene_files
      - Replace `wiring_files` variable with `scene_files` in the merge: `all_files = {**gm_files, **node_files, **scene_files}`
    - Keep `_strip_node_tscn()` call (safety net per CONTEXT.md decision) but it should be a no-op now since prompt no longer asks for .tscn
    - Update intermediate dump: rename "4_wiring_files" dir to "4_scene_files" for clarity
    - Remove the `AsyncAnthropic` import from wiring_generator since it's no longer used in the wiring call path

    **wiring_generator.py changes:**
    - Remove `run_wiring_generator()` function entirely
    - Remove `_build_wiring_system_prompt()` function
    - Remove `SONNET_MODEL` constant
    - Remove `AsyncAnthropic` import, `json` import
    - Keep: `_patch_project_godot_autoloads()`, `_HARDCODED_AUTOLOAD_NAMES`, `TEMPLATE_DIR`, `_REPO_ROOT`
    - Keep: `re`, `Path`, `logging` imports as needed by remaining code

    **node_generator.py changes:**
    - In `_build_node_system_prompt()`:
      1. Remove the `if node.scene_path: parts.append(f"Also generate: {node.scene_path}")` block (lines 153-154)
      2. Add viewport size context BEFORE the Godot 4 syntax line:
         ```
         "The game viewport design resolution is 1152x648 pixels.\n"
         "Use get_viewport().get_visible_rect().size to read dimensions at runtime\n"
         "rather than hardcoding pixel values."
         ```

    **project.godot changes:**
    - Add `[display]` section at the end of the file (after [rendering]):
      ```
      [display]

      window/size/viewport_width=1152
      window/size/viewport_height=648
      window/stretch/mode="canvas_items"
      window/stretch/aspect="expand"
      ```
  </action>
  <verify>
    <automated>cd /Users/albertluo/other/moonpond/backend && uv run python -m pytest backend/tests/test_tscn_builder.py backend/tests/test_scene_assembler.py -x -q</automated>
  </verify>
  <done>Pipeline uses SceneAssembler for Stage 4. Node generator prompt has viewport hint and no .tscn instruction. project.godot has [display] section. TscnBuilder/SceneAssembler tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update existing test suite for removed wiring LLM call</name>
  <files>backend/backend/tests/test_wiring_generator.py, backend/backend/tests/test_contract_pipeline.py</files>
  <action>
    **test_wiring_generator.py changes:**
    - Remove all tests that call `run_wiring_generator()` (test_produces_main_tscn, test_main_tscn_has_ext_resources, test_ext_resource_ids_unique, test_each_node_has_scene_entry, test_no_autoloads_no_project_godot, test_autoloads_produces_project_godot, test_project_godot_preserves_input_section, test_emits_stage_start)
    - Remove `_build_wiring_system_prompt` from imports (function deleted)
    - Remove `run_wiring_generator` from imports (function deleted)
    - Keep autoload dedup tests (test_gamemanager_autoload_not_duplicated, test_non_gamemanager_autoloads_still_added, test_empty_autoloads_only_gamemanager) -- these test `_patch_project_godot_autoloads` which is still alive
    - Remove dynamic node filtering tests that tested `_build_wiring_system_prompt` (test_dynamic_nodes_excluded_from_wiring_prompt, test_all_static_nodes_included_in_wiring_prompt) -- this behavior is now in SceneAssembler and covered by test_scene_assembler.py
    - Update imports to only import `_patch_project_godot_autoloads` from wiring_generator
    - Remove unused imports (json, AsyncMock, etc.) and unused fixtures (_mock_client, three_node_contract, generated_files, _SAMPLE_MAIN_TSCN)

    **test_contract_pipeline.py changes:**
    - Remove 5th LLM response (`_MAIN_TSCN`) from the `side_effect` list in both `test_contract_pipeline_full_flow` and `test_contract_pipeline_saves_intermediates` -- pipeline now makes 4 LLM calls (spec, contract, player node, enemy node) + 1 game_manager_generator call. Check if game_manager_generator uses its own client or the pipeline's client:
      - Looking at pipeline.py: `generate_game_manager_script_async(self._client, contract)` -- it uses the same client. So we need to check if it makes an LLM call.
      - If game_manager_generator makes an LLM call, the side_effect needs: spec, contract, game_manager, player_node, enemy_node (5 calls but different order, no wiring).
      - If game_manager_generator does NOT make an LLM call (it was implemented as template-based in Phase 5.1), side_effect needs only 4: spec, contract, player_node, enemy_node.
    - To determine which: read game_manager_generator.py. Based on Phase 5.1 decision "Hardcoded template base as string constant in generator (no filesystem read)" and function name `generate_game_manager_script_async` -- this likely DOES use LLM. Add a mock response for game_manager_generator LLM call (a simple GDScript string) as the 3rd response in the side_effect list. The total should be 5: spec, contract, game_manager, player, enemy. But verify by checking the actual source.
    - Remove `_MAIN_TSCN` constant (no longer needed)
    - Update assertion `assert len(stage_starts) >= 5` -- count may change since wiring stage_start is replaced by "Assembling scene files..." from the pipeline itself. The number of stage_starts should still be >= 5 (pipeline start, spec, contract, game_manager, node gen waves, assembling scenes).
    - Update intermediate file check: change `assert (dump_dir / "4_wiring_files").is_dir()` to `assert (dump_dir / "4_scene_files").is_dir()`
    - Mock SceneAssembler if needed, or let it run since it's deterministic (no LLM). Since the contract has scene_path=None for both nodes, SceneAssembler will produce Main.tscn with inline nodes -- this is fine, no mock needed.
  </action>
  <verify>
    <automated>cd /Users/albertluo/other/moonpond/backend && uv run python -m pytest -x -q</automated>
  </verify>
  <done>Full test suite passes. No test references run_wiring_generator or _build_wiring_system_prompt. Pipeline integration test has correct number of LLM mock responses (no wiring call). Intermediate dump uses "4_scene_files" dir name.</done>
</task>

</tasks>

<verification>
cd /Users/albertluo/other/moonpond/backend && uv run python -m pytest -x -q
</verification>

<success_criteria>
- Full test suite passes (0 failures)
- ContractPipeline.generate() no longer calls run_wiring_generator
- Node generator prompt includes "1152x648" and does NOT include "Also generate:"
- project.godot contains [display] section with viewport_width=1152
- wiring_generator.py only contains _patch_project_godot_autoloads (LLM code removed)
- Pipeline makes one fewer LLM API call than before
</success_criteria>

<output>
After completion, create `.planning/phases/06-programmatic-tscn-generation-and-display-configuration/06-02-SUMMARY.md`
</output>
