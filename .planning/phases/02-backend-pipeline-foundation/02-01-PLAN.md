---
phase: 02-backend-pipeline-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/__init__.py
  - backend/pipelines/__init__.py
  - backend/pipelines/base.py
  - backend/pipelines/registry.py
  - backend/models/__init__.py
  - backend/models/requests.py
  - backend/models/responses.py
  - backend/state.py
  - backend/godot/__init__.py
  - backend/godot/runner.py
autonomous: true
requirements:
  - PIPE-04
  - PIPE-05

must_haves:
  truths:
    - "Pipeline registry resolves 'stub' to a concrete class that satisfies GamePipeline Protocol"
    - "Godot runner executes headless export without blocking the asyncio event loop"
    - "Godot runner validates output file existence, not exit code"
    - "Godot runner captures stderr from the subprocess"
  artifacts:
    - path: "backend/pipelines/base.py"
      provides: "GamePipeline Protocol, ProgressEvent, GameResult, EmitFn type alias"
      exports: ["GamePipeline", "ProgressEvent", "GameResult", "EmitFn"]
    - path: "backend/pipelines/registry.py"
      provides: "PIPELINES dict mapping strategy names to pipeline classes"
      exports: ["PIPELINES"]
    - path: "backend/models/requests.py"
      provides: "GenerateRequest Pydantic model"
      exports: ["GenerateRequest"]
    - path: "backend/models/responses.py"
      provides: "GenerateResponse Pydantic model"
      exports: ["GenerateResponse"]
    - path: "backend/state.py"
      provides: "active_jobs dict for job queue storage"
      exports: ["active_jobs"]
    - path: "backend/godot/runner.py"
      provides: "run_headless_export async function, RunResult dataclass"
      exports: ["run_headless_export", "RunResult"]
  key_links:
    - from: "backend/pipelines/registry.py"
      to: "backend/pipelines/base.py"
      via: "imports GamePipeline Protocol for type annotation"
      pattern: "from .base import"
    - from: "backend/godot/runner.py"
      to: "asyncio.create_subprocess_exec"
      via: "non-blocking subprocess execution"
      pattern: "asyncio\\.create_subprocess_exec"
---

<objective>
Set up the backend Python project and create all foundational modules: Pydantic models, GamePipeline Protocol with registry, job state store, and async Godot headless runner.

Purpose: Establish the type contracts and core building blocks that Plan 02 will wire into API endpoints. Every module created here is imported by the API layer and stub pipeline.
Output: A `backend/` Python package with pyproject.toml, models, pipeline protocol/registry, state module, and Godot runner — all importable but not yet served via HTTP.
</objective>

<execution_context>
@/Users/albertluo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/albertluo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backend-pipeline-foundation/02-CONTEXT.md
@.planning/phases/02-backend-pipeline-foundation/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Python project setup and type contracts</name>
  <files>backend/pyproject.toml, backend/__init__.py, backend/pipelines/__init__.py, backend/pipelines/base.py, backend/models/__init__.py, backend/models/requests.py, backend/models/responses.py, backend/state.py</files>
  <action>
1. Create `backend/pyproject.toml` with uv + FastAPI configuration:
   - name: "moonpond-backend", version: "0.1.0", requires-python: ">=3.12"
   - dependencies: ["fastapi[standard]>=0.135.0", "aiofiles"]
   - optional-dependencies dev: ["pytest", "pytest-anyio", "httpx", "anyio"]
   - [tool.pytest.ini_options]: asyncio_mode = "auto"
   - [tool.uv.scripts]: dev = "uvicorn backend.main:app --reload --port 8000"

2. Create `backend/__init__.py` (empty).

3. Create `backend/pipelines/__init__.py` (empty).

4. Create `backend/pipelines/base.py` with:
   - `ProgressEvent(BaseModel)`: fields `type: str` (stage_start|stage_complete|error|done), `message: str`, `data: dict = {}`
   - `GameResult(BaseModel)`: fields `job_id: str`, `wasm_path: str`, `controls: list[dict] = []`
   - `EmitFn = Callable[[ProgressEvent], Awaitable[None]]` type alias
   - `GamePipeline(Protocol)`: async method `generate(self, prompt: str, job_id: str, emit: EmitFn) -> GameResult`

5. Create `backend/models/__init__.py` (empty).

6. Create `backend/models/requests.py` with:
   - `GenerateRequest(BaseModel)`: field `prompt: str`

7. Create `backend/models/responses.py` with:
   - `GenerateResponse(BaseModel)`: field `job_id: str`

8. Create `backend/state.py` with:
   - `active_jobs: dict[str, asyncio.Queue] = {}` (module-level dict, Queue values created inside async handlers per Pitfall 2)
   - TYPE_CHECKING import for ProgressEvent to avoid circular imports

9. Run `cd backend && uv sync` to install all dependencies.
  </action>
  <verify>
    <automated>cd /Users/albertluo/other/moonpond/backend && uv sync && uv run python -c "from backend.pipelines.base import GamePipeline, ProgressEvent, GameResult, EmitFn; from backend.models.requests import GenerateRequest; from backend.models.responses import GenerateResponse; from backend.state import active_jobs; print('All imports OK')"</automated>
  </verify>
  <done>All backend modules import cleanly. pyproject.toml installs fastapi>=0.135.0, aiofiles, and dev deps. ProgressEvent, GameResult, GamePipeline Protocol, GenerateRequest, GenerateResponse, and active_jobs are all importable.</done>
</task>

<task type="auto">
  <name>Task 2: Pipeline registry and Godot headless runner</name>
  <files>backend/pipelines/registry.py, backend/godot/__init__.py, backend/godot/runner.py</files>
  <action>
1. Create `backend/godot/__init__.py` (empty).

2. Create `backend/godot/runner.py` with:
   - `GODOT_BIN` constant: read from `os.environ.get("GODOT_BIN")` with fallback to `Path(__file__).parent.parent.parent / "godot" / "Godot.app" / "Contents" / "MacOS" / "Godot"` (relative to repo root, matching Phase 1 setup)
   - `RunResult` dataclass: `success: bool`, `stderr: str`, `output_path: Path | None`
   - `async def run_headless_export(project_path: Path, output_dir: Path, preset_name: str = "Web") -> RunResult`:
     - Set `output_path = output_dir / "index.html"`
     - `output_dir.mkdir(parents=True, exist_ok=True)`
     - Use `asyncio.create_subprocess_exec` with GODOT_BIN, "--headless", "--path", str(project_path), "--export-release", preset_name, str(output_path)
     - stdout=DEVNULL, stderr=PIPE
     - `await proc.communicate()` for non-blocking wait
     - Decode stderr (errors="replace")
     - Validate `output_path.exists()` for success (NOT exit code — Godot issue #83042)
     - Return `RunResult(success=success, stderr=stderr, output_path=output_path if success else None)`

3. Create `backend/pipelines/registry.py` with:
   - Import will be filled when stub pipeline exists in Plan 02
   - For now: `PIPELINES: dict[str, type] = {}` (empty dict, Plan 02 adds the stub entry)
   - `def get_pipeline(name: str) -> type` that returns `PIPELINES[name]` or raises KeyError with helpful message listing available pipelines
  </action>
  <verify>
    <automated>cd /Users/albertluo/other/moonpond/backend && uv run python -c "from backend.godot.runner import run_headless_export, RunResult, GODOT_BIN; from backend.pipelines.registry import PIPELINES, get_pipeline; print(f'GODOT_BIN={GODOT_BIN}'); print(f'Registry keys: {list(PIPELINES.keys())}'); print('Runner and registry OK')"</automated>
  </verify>
  <done>Godot runner uses asyncio.create_subprocess_exec (non-blocking), validates output file existence (not exit code), captures stderr. GODOT_BIN reads from env var with fallback. Registry exports PIPELINES dict and get_pipeline function.</done>
</task>

</tasks>

<verification>
- All Python modules import without errors
- `uv sync` installs fastapi>=0.135.0, aiofiles, and dev deps
- GamePipeline Protocol defines async generate(prompt, job_id, emit) -> GameResult
- ProgressEvent and GameResult are Pydantic models with correct fields
- Godot runner uses asyncio.create_subprocess_exec (grep for it)
- Runner validates file existence, not exit code (grep for `.exists()`)
</verification>

<success_criteria>
- `backend/pyproject.toml` exists and `uv sync` succeeds
- All 8 Python modules are importable
- Type contracts (GamePipeline, ProgressEvent, GameResult, EmitFn, GenerateRequest, GenerateResponse) are defined and exported
- Godot runner is async and non-blocking
- Pipeline registry is ready to accept pipeline registrations
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-pipeline-foundation/02-01-SUMMARY.md`
</output>
