---
phase: 07-agentic-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/backend/pipelines/agentic/__init__.py
  - backend/backend/pipelines/agentic/models.py
  - backend/backend/pipelines/agentic/spec_generator.py
  - backend/backend/pipelines/agentic/file_generator.py
  - backend/backend/tests/test_agentic_models.py
autonomous: true
requirements:
  - AGNT-02
  - AGNT-03
  - AGNT-05
  - AGNT-06

must_haves:
  truths:
    - "AgenticGameSpec Pydantic model validates a well-formed game spec JSON"
    - "VerifierError and VerifierResult models validate structured verifier output"
    - "VerifierResult.has_critical_errors returns True when any error has severity critical"
    - "write_file tool dispatch writes a file to disk and updates generated_files dict"
    - "read_file tool dispatch returns content of an existing file or an error message"
    - "Tool dispatch handles unknown tool names gracefully with an error string"
  artifacts:
    - path: "backend/backend/pipelines/agentic/models.py"
      provides: "AgenticGameSpec, VerifierError, VerifierResult Pydantic models"
      exports: ["AgenticGameSpec", "VerifierError", "VerifierResult"]
    - path: "backend/backend/pipelines/agentic/spec_generator.py"
      provides: "Spec generation from prompt via LLM (first conversation turn)"
      exports: ["run_spec_generator"]
    - path: "backend/backend/pipelines/agentic/file_generator.py"
      provides: "Tool definitions, tool dispatch, multi-turn file generation loop"
      exports: ["WRITE_FILE_TOOL", "READ_FILE_TOOL", "AGENT_TOOLS", "_dispatch_tool", "run_file_generation"]
    - path: "backend/backend/tests/test_agentic_models.py"
      provides: "Unit tests for models and tool dispatch"
  key_links:
    - from: "backend/backend/pipelines/agentic/file_generator.py"
      to: "filesystem"
      via: "Path.write_text in _dispatch_tool"
      pattern: "game_dir.*write_text"
    - from: "backend/backend/pipelines/agentic/spec_generator.py"
      to: "anthropic"
      via: "client.messages.create + model_validate"
      pattern: "messages\\.create"
---

<objective>
Create the agentic pipeline's data models, spec generator, and file generation tooling — the foundational types and functions that the pipeline orchestrator (Plan 02/03) will consume.

Purpose: Establish the type contracts (AgenticGameSpec, VerifierError, VerifierResult), tool definitions (write_file, read_file), tool dispatch function, and spec generation stage. These are the building blocks the agent loop and verifier depend on.

Output: `pipelines/agentic/` package with models.py, spec_generator.py, file_generator.py, and comprehensive test coverage.
</objective>

<execution_context>
@/Users/albertluo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/albertluo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-agentic-pipeline-lightweight-agent-loop-pipeline-with-spec-generation-todo-driven-iterative-file-generation-and-verifier-agent/07-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From backend/backend/pipelines/base.py:
```python
class ProgressEvent(BaseModel):
    type: str  # stage_start | stage_complete | error | done
    message: str
    data: dict = {}

class GameResult(BaseModel):
    job_id: str
    wasm_path: str
    controls: list[dict] = []

EmitFn = Callable[[ProgressEvent], Awaitable[None]]

class GamePipeline(Protocol):
    async def generate(
        self, prompt: str, job_id: str, emit: EmitFn,
        *, save_intermediate: bool = True,
    ) -> GameResult: ...
```

From backend/backend/pipelines/exporter.py:
```python
async def run_exporter(
    game_dir: str, files: dict[str, str], controls: list[dict], emit: EmitFn,
) -> GameResult:
```

From backend/backend/pipelines/registry.py:
```python
PIPELINES: dict[str, type] = { "stub": ..., "multi_stage": ..., "contract": ... }
def get_pipeline(name: str) -> type:
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Agentic data models and spec generator</name>
  <files>
    backend/backend/pipelines/agentic/__init__.py,
    backend/backend/pipelines/agentic/models.py,
    backend/backend/pipelines/agentic/spec_generator.py,
    backend/backend/tests/test_agentic_models.py
  </files>
  <behavior>
    - AgenticGameSpec validates a dict with title, genre, mechanics, entities, scene_description, win_condition, fail_condition fields
    - AgenticGameSpec rejects dict missing required fields (ValidationError)
    - VerifierError validates dict with file_path, error_type (Literal), description, severity (Literal)
    - VerifierResult validates dict with errors list + summary string
    - VerifierResult.has_critical_errors returns True when any error has severity "critical"
    - VerifierResult.has_critical_errors returns False when all errors are "warning" or list is empty
    - run_spec_generator calls client.messages.create with system prompt + user prompt, parses JSON response into AgenticGameSpec
  </behavior>
  <action>
    Create `backend/backend/pipelines/agentic/__init__.py` (empty).

    Create `backend/backend/pipelines/agentic/models.py` with:
    - `AgenticGameSpec(BaseModel)` — fields: title (str), genre (str), mechanics (list[str]), entities (list[dict]), scene_description (str), win_condition (str), fail_condition (str). This is agentic-native per user decision — do NOT reuse RichGameSpec from contract pipeline.
    - `VerifierError(BaseModel)` — fields: file_path (str), error_type (Literal["syntax", "reference", "logic", "missing"]), description (str), severity (Literal["critical", "warning"])
    - `VerifierResult(BaseModel)` — fields: errors (list[VerifierError]), summary (str). Add `has_critical_errors` property that returns `any(e.severity == "critical" for e in self.errors)`.

    Create `backend/backend/pipelines/agentic/spec_generator.py` with:
    - `SPEC_SYSTEM_PROMPT` constant: instructs the LLM to analyze a game prompt and produce a JSON object matching AgenticGameSpec fields. Include instruction to respond ONLY with valid JSON, no markdown fences.
    - `async def run_spec_generator(client: AsyncAnthropic, prompt: str, emit: EmitFn) -> AgenticGameSpec`: emits stage_start event, calls `client.messages.create()` with model `claude-sonnet-4-6`, max_tokens=4096, system=SPEC_SYSTEM_PROMPT, messages=[{"role": "user", "content": prompt}]. Extracts `response.content[0].text`, does `json.loads()` + `AgenticGameSpec.model_validate()`. Follow the project convention from contract/spec_expander.py (json.loads + model_validate, NOT messages.parse).

    Create `backend/backend/tests/test_agentic_models.py` with tests for all behaviors listed above. Mock `AsyncAnthropic.messages.create` using `AsyncMock` for the spec_generator test (follow pattern from test_contract_generator.py).
  </action>
  <verify>
    <automated>cd /Users/albertluo/other/moonpond/backend && uv run pytest backend/tests/test_agentic_models.py -x -v</automated>
  </verify>
  <done>
    AgenticGameSpec, VerifierError, VerifierResult models pass validation tests. run_spec_generator correctly parses mocked LLM response into AgenticGameSpec. has_critical_errors property works for both critical and non-critical error lists.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tool definitions and dispatch function</name>
  <files>
    backend/backend/pipelines/agentic/file_generator.py,
    backend/backend/tests/test_agentic_models.py
  </files>
  <behavior>
    - _dispatch_tool("write_file", {"filename": "player.gd", "content": "extends Node2D"}, game_dir, {}) writes file to game_dir/player.gd and updates generated_files dict
    - _dispatch_tool("read_file", {"filename": "player.gd"}, game_dir, {"player.gd": "extends Node2D"}) returns file content from generated_files dict
    - _dispatch_tool("read_file", {"filename": "missing.gd"}, game_dir, {}) returns error string when file not in dict and not on disk
    - _dispatch_tool("read_file", {"filename": "on_disk.gd"}, game_dir, {}) reads from disk if not in generated_files but file exists on disk
    - _dispatch_tool("unknown_tool", ...) returns error string
    - WRITE_FILE_TOOL and READ_FILE_TOOL dicts have correct name, description, input_schema with required fields
  </behavior>
  <action>
    Create `backend/backend/pipelines/agentic/file_generator.py` with:
    - `WRITE_FILE_TOOL` dict: name="write_file", description explains one file per turn + bare filename, input_schema with required filename+content properties (copy from research code example exactly).
    - `READ_FILE_TOOL` dict: name="read_file", description for inspecting previously written files, input_schema with required filename property.
    - `AGENT_TOOLS = [WRITE_FILE_TOOL, READ_FILE_TOOL]` list.
    - `async def _dispatch_tool(tool_name: str, tool_input: dict, game_dir: Path, generated_files: dict[str, str]) -> str`: handles write_file (write to disk + update dict, return OK message), read_file (check dict first, then disk, return content or error), unknown tool (return error). Wrap write_file in try/except and return error string on failure per research pitfall guidance.
    - `GENERATOR_MODEL = "claude-sonnet-4-6"` constant.
    - `MAX_TURNS_PER_ITERATION = 30` constant.

    Do NOT implement `run_file_generation` yet — that depends on the pipeline orchestrator in Plan 02. Only implement the tool infrastructure.

    Add tests for all dispatch behaviors to `test_agentic_models.py` (or create a separate test section in the same file). Use `tmp_path` fixture for game_dir.
  </action>
  <verify>
    <automated>cd /Users/albertluo/other/moonpond/backend && uv run pytest backend/tests/test_agentic_models.py -x -v</automated>
  </verify>
  <done>
    Tool definitions are well-formed dicts with correct schemas. _dispatch_tool correctly handles write_file (disk write + dict update), read_file (dict lookup, disk fallback, missing error), and unknown tools. All tests pass.
  </done>
</task>

</tasks>

<verification>
- All Pydantic models instantiate from valid dicts and reject invalid input
- Tool dispatch writes/reads files correctly on the filesystem
- Mocked LLM spec generator produces AgenticGameSpec
- No import errors from the new agentic package
</verification>

<success_criteria>
- `backend/backend/pipelines/agentic/` package exists with models.py, spec_generator.py, file_generator.py
- `uv run pytest backend/tests/test_agentic_models.py -x` passes all tests
- Models, tool defs, and dispatch function are importable from the package
</success_criteria>

<output>
After completion, create `.planning/phases/07-agentic-pipeline-lightweight-agent-loop-pipeline-with-spec-generation-todo-driven-iterative-file-generation-and-verifier-agent/07-01-SUMMARY.md`
</output>
